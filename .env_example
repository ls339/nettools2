MONGO_ROOT_USERNAME=root
MONGO_ROOT_PASSWORD=change_me
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=change_me
# API_KEY=change_me         # Uncomment and set to enable API key authentication
# ALLOW_PRIVATE_SCAN=true   # Uncomment to allow scanning private/loopback IP addresses (e.g. home network)
AUTH_SECRET=change_me_to_random_32_chars
AUTH_USERNAME=admin
AUTH_PASSWORD_HASH=              # bcrypt hash — generate with: node -e "console.log(require('bcryptjs').hashSync('yourpassword', 10))"
